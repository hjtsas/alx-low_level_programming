#include <stdio.h>

/**
 * swap_int - swaps the values of two int
 *@a: int
 *@b: int
 *
 * return (0)
 */

void swap_int(int *a, int *b)
{
	int x, y;

	x = y;
	y = temp;

	_putchar('x = %d, y =%d\n', x, y);
	return(0);
}
