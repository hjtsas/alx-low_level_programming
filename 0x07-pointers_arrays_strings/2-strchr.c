#include <stdio.h>
#include <string.h>

/**
 * _strchr - locate a character in string
 * @c: character
 * @s: string
 * Return 0
 */

char *_strchr(char *s, char c)
{
	int a;
	for (a = 0, a < '\0', a++)
	{
		s[a] == c;
	}
	return ('\0');
}
